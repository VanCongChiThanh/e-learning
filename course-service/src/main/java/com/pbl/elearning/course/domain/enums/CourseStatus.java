package com.pbl.elearning.course.domain.enums;

public enum CourseStatus {
    DRAFT,       // Course is in draft state, not yet published
    PUBLISHED,   // Course is published and available to students
    ARCHIVED,    // Course is archived and no longer active
    DELETED      // Course has been deleted
}
